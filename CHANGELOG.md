# Changelog

本文件记录论文助手（论文智能研究与写作助手）的版本更新历史。

> 说明：v1.6.0 及更早版本的源码与打包文件均已丢失，仅根据旧 README 中保留的功能说明整理，详情无法核对。相关条目已明确标注。

## [v2.3.0] - 2026-08-19

### 新增：自动判定结果
- 全自动论文现在会把开始时填写的要求转成自动质量门，统一检查事实核查、正文引用、参考文献对应、论文结构、摘要、字数、资料完整性和 AI/重复风险阈值。
- 生成完成后明确显示“可以导出”或“自动判定未达标”，并列出未达标原因；程序不会因为某个 AI 检查步骤失败或被跳过而显示通过。
- 质量报告、完成窗口和全自动摘要使用同一套判定结果，避免不同页面显示不一致。

### 新增：稳定数据与升级保护
- 用户数据默认保存到 `%LOCALAPPDATA%\PaperAssistant\paper_project`，不再随单文件 EXE 或完整版目录分叉。
- 首次升级会自动兼容桌面单文件版、完整版和旧程序目录中的已有论文、模型配置、历史记录、证据、批注和输出文件。
- 同名文件不会被静默覆盖；冲突副本会保存在本机迁移备份目录，旧数据源不会删除，迁移报告不包含 API Key 内容。

### 新增：版本检查
- 程序启动后会在后台检查 GitHub 最新 Release，不阻塞论文编辑和生成。
- “帮助 → 检查更新”会显示当前版本、最新版本、真实 Release 地址以及网络或 HTTP 错误；发现新版本时可直接打开下载页选择单文件版或完整版。

### 修复与稳定性
- 修复全自动导出目录不可用时的默认目录回退崩溃。
- Claude 和 Gemini 请求回归验证现在覆盖实际模型 ID、系统提示、消息角色和认证头。
- Windows 打包脚本改用稳定的系统 PowerShell 调用和 ASCII 传输文件名，避免中文路径/编码导致构建末尾失败；发布流程补充 Windows 测试、打包和 Tag Release 自动化配置。

### 使用提示
- 自动判定未达标的结果仍会保存报告和运行日志，便于程序继续优化或重新运行；它不会被标记为满足起始要求。
- 政府站点反爬、CNKI 登录限制和超长论文耗时仍属于外部环境或设计限制，程序会保留真实失败状态，不会伪造检索结果。

## [v2.2.0] - 2026-08-18

### Added
- 新增“批注管理”页面，可统一查看、搜索、筛选、新增、编辑和批量删除论文批注。
- 分章节写作页支持选中正文后直接添加批注，并自动把批注标记写回当前章节。
- 新增批注样式模板，支持自定义编号前缀、分隔符、位数、类别、颜色和说明。
- 新增普通批注、待办批注、术语说明等默认样式，并支持按样式重新编号。
- 新增批注表导出；全自动模式会输出 `批注表.json` 和 `批注表.md`。

### Changed
- 旧 `E001` 证据编号会无损同步为证据批注，证据库仍保持独立，不会因删除普通批注而误删来源资料。
- 正文中的非证据批注可点击查看详情，使用到的批注会随论文导出到“批注说明”附录。
- 批注重新编号时会同步更新各章节中的对应标记，保持正文与批注表一致。
- AI 自然化和定向修改会同时保护证据引用与批注标记，防止改写过程中丢失编号。
- 历史记录会关联批注表、证据表、质量报告和运行日志等完整导出文件。
- GitHub Release 资产从本版本起统一使用中文显示名，并明确区分“单文件版”和“完整版”；底层下载文件使用 GitHub 要求的 ASCII 兼容名。

### Fixed
- 证据引用解析现在兼容 `[E001]`、`[E-001]` 和 `[E_001]`，并统一映射到原证据记录。
- 修复高级工作台仅识别固定 `[E001]` 格式、导致兼容编号被误判为无引用的问题。

## [v2.1.2] - 2026-08-18

### Fixed
- 修复 OpenAlex 学术检索遇到请求过于频繁时立即失败的问题，现在会自动等待并重试。
- 修复 OpenAlex 限流后的错误提示过于笼统的问题；如果服务仍在限流，会明确提示稍后重试。
- 减少连续检索请求对 OpenAlex 服务的冲击，提升多关键词检索和全自动论文流程的稳定性。
- 支持通过 OpenAlex 联系邮箱配置提高礼貌访问稳定性。
- 修复检索未获得任何可靠资料时仍继续生成空资料论文的问题；现在会先停止并提示补充资料或稍后重试。

## [v2.1.1] - 2026-08-18

### Added
- AI 模型中心的 Provider 列表和模型列表现在都提供直观的启用复选框，勾选状态会立即保存并参与模型路由。
- 模型中心新增启用/停用、删除模型配置、预设库管理入口；删除和覆盖操作都会显示二次确认。
- 模型中心按钮、Provider、模型状态和常用操作增加少量 Emoji 标识，便于快速区分刷新、测试、收藏、启用和删除操作。

### Changed
- Provider 和模型状态统一使用“已启用/已停用”文案，避免把配置状态与程序启动状态混淆。
- 自定义 OpenAI Compatible Provider 可以直接添加中转站模型；同名模型不会再静默覆盖，只有明确确认后才会替换已有配置。
- API Key 保存后会显示凭据状态，并通过 Windows Credential Manager 保存和校验，不再静默失败。

### Fixed
- 修复 DeepSeek 刷新模型列表失败的问题。
- 修复已经配置 API Key 但首次运行仍提示未检测到可用 Key 的问题。
- 修复升级后已保存的 DeepSeek API Key 可能无法被识别的问题。
- 修复导入预设模型时直接覆盖已有模型配置的问题。
- 修复模型启用状态没有持久化、重启后无法正确参与路由的问题。

### 使用说明
- 在“设置 → AI 模型中心”中，勾选 Provider 和具体模型即可启用；取消勾选即可停用。
- 添加中转站时，先添加 Provider 并填写 Base URL/API Key，再点击“添加模型”填写中转站的 Model ID。
- 如果预设与已有模型使用相同的 Provider ID 和 Model ID，系统会先展示覆盖确认，不会自动替换原配置。

## [v2.1.0] - 2026-08-18

### Added
- AI 模型中心：动态模型发现、模型搜索、能力筛选、收藏和连接测试。
- 参考文献中心：RIS/BibTeX/CSL JSON/本地资料导入、文献去重和真实引用映射。
- Zotero Local/Web 连接与 Gemini Notebook Enterprise 官方资料发送。
- 统一 UI 主题、参考文献中心、模型中心和关于页面。

### Changed
- 普通、全自动、高级模式统一使用新的模型选择系统。

### Fixed
- 修复旧模型配置和引用数据在升级过程中的兼容问题。

## [v2.0.1] - 2026-08-17

### Fixed

- 修复部分 Windows 环境下打包脚本无法正确执行的问题
- 修复升级后旧版 API 配置、论文、证据和历史记录可能无法自动保留的问题
- 修复下载文件格式不统一的问题；本版本同时提供 EXE 和 ZIP

## [v2.0.0] - 2026-08-16

### Added

- **高级模式工作台**：题目 → 提纲 → 初稿 → 论证 → 结构优化 → 终稿 六阶段流程化指导，每阶段独立执行与检查（`core/advanced_state.py`、`gui/pages/advanced_workspace.py`）
- **论文历史记录**：每次写作任务自动保存归档，可回看、继续任意历史任务（`core/history.py`、`gui/pages/history_page.py`）
- **定向修改**：可对指定章节/段落单独提出修改要求（`core/targeted_edit.py`）
- **自定义输出目录**：产出文件可指定保存位置（`core/output_location.py`）
- **模型预设方案**：常用模型配置一键切换（`core/model_presets.py`）

### Improved

- 全自动模式：21 步流水线支持自动重试、自动保存、暂停/取消、断点恢复（崩溃或关机后重启可继续）
- 响应式窗口与高 DPI 支持（不同分辨率/缩放比例下界面正常）
- 写作质量检测与自然化修改：事实/数字/引用编号/参考文献保护机制强化
- 多格式导出：导出后自动验证文件结构与内容，失败自动重生成
- 多模型供应商系统：OpenAI 兼容（DeepSeek/OpenAI/通义千问/Moonshot/智谱/OpenRouter/Ollama/自定义）、Claude、Gemini 统一接口，支持模型路由与备用切换

### Known Issues

- 首次启动单文件 EXE 需 10~30 秒（PyInstaller 解压）
- 政府网页检索受目标站点反爬影响，可能失败（程序自动跳过并提示）
- CNKI 受登录限制，仅支持手动录入与 RIS/BibTeX 导入

---

## [v1.6.0] - 2026-08-16（历史版本，源码未保留）

### Added

- **多格式导出系统**：统一 Document 模型，一份内容导出 6 种格式（DOCX/PPTX/PDF/TXT/Markdown/HTML），均可脱离本程序独立打开
- DOCX 支持用户格式要求：字体/字号/行距/首行缩进/页边距真实写入
- PPTX 封面 + 各章 + 参考文献真实可编辑幻灯片
- PDF 内嵌中文字体
- 导出页支持多格式同时导出、自定义文件名（自动清理 Windows 非法字符）、自动追加姓名/日期
- 导出后自动验证（失败自动重生成一次）
- 输出文件夹管理：双击打开、删除选中、清空全部输出

## [v1.5.0] - 2026-08-16（历史版本，源码未保留）

### Added

- **多模型供应商系统**：统一 `LLMProvider` 接口（OpenAI Compatible / Anthropic Claude / Google Gemini / 本地 Ollama），可添加自定义模型
- Model Router：按任务选择模型，失败自动切换备用模型
- 模型方案一键切换、模型健康检查、API Key 本地保存（界面掩码显示，不进日志）
- 成本控制：单次最大调用次数 / Token / 预算（估算），达到限制自动暂停优化
- 检测服务 Provider 接口：内置启发式分析 + 自定义检测 API（未接入正规第三方 API 时明确标注"内部分析值"）

## 更早版本（v1.0.0 至 v1.4.x）

Historical version — detailed changelog unavailable. 源码与记录均未保留，无法确认具体更新内容。
