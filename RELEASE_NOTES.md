# 论文助手 v2.3.0

## 新增：自动判定论文是否符合起始要求

- 全自动论文模式会读取开始时填写的论文要求，并在生成结束后自动检查资料完整性、论文结构、摘要、字数、事实核查、正文引用、参考文献对应关系以及 AI/重复风险阈值。
- 所有检查通过时显示“可以导出”；任何检查失败、跳过、资料不足或无法判定时显示“自动判定未达标”，不会把未完成结果误报为合格。
- 质量报告、完成窗口和全自动摘要使用同一份判定结果，并列出未达标原因。结果仍会保存，方便重新运行或继续优化，但不再被标记为满足起始要求。

## 新增：升级后保护已有论文和配置

- 用户数据默认保存到 `%LOCALAPPDATA%\\PaperAssistant\\paper_project`，单文件版和完整版共享同一份论文、历史记录、证据、批注、模型配置和导出文件。
- 首次升级会自动兼容旧版单文件版、完整版和旧程序目录中的数据；旧数据只复制不删除。
- 同名文件不会被静默覆盖，冲突副本会保存在迁移备份目录；迁移报告不会写入 API Key 或其他凭据内容。

## 新增：应用内检查更新

- 程序启动后会在后台检查 GitHub 最新 Release，不阻塞论文编辑和生成。
- “帮助 → 检查更新”会显示当前版本、最新版本、真实 Release 地址以及网络或 HTTP 错误。
- 发现新版本时可以直接打开下载页，选择单文件版或完整版。

## 修复与稳定性

- 修复自定义导出目录不可用时的默认目录回退崩溃。
- 增强 Claude 和 Gemini 请求校验，确保实际请求使用用户选择的模型和正确的消息格式。
- Windows 打包改用更稳定的系统 PowerShell 调用和兼容文件名，减少中文路径导致的构建失败。
- 发布流程现在同时验证 Windows 构建、版本 Tag、Release 和下载资产。

## 已知限制

- 政府站点可能受到反爬或网络环境影响，程序会保留真实失败状态。
- CNKI 仍受登录、验证码和付费墙限制，支持手动录入以及 RIS/BibTeX 导入。
- 长论文全自动生成可能耗时较长，可使用断点恢复继续任务。

# 版本: v2.3.0 (2026-08-19) 更新: 修正客户 Release 日志重复内容
