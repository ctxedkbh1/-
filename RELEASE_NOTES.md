### Added
- 新增“批注管理”页面，支持搜索、筛选、新增、编辑、批量删除和重新编号。
- 分章节写作页支持选中正文后直接添加批注，并自动写入批注标记。
- 新增可自定义编号前缀、分隔符、位数、类别和颜色的批注样式模板。
- 全自动导出新增 `批注表.json` 和 `批注表.md`，正文使用到的批注会进入“批注说明”附录。

### Changed
- 旧 `E001` 证据编号会无损同步到批注库，证据库与普通批注仍保持独立。
- AI 自然化和定向修改会保护批注标记，防止改写过程中丢失编号。
- 批注重新编号会同步更新各章节正文中的标记。
- 历史记录现在会关联批注表等完整导出文件。
- GitHub 下载资产统一使用中文显示名，并区分“单文件版”和“完整版”。

### Fixed
- 证据引用兼容 `[E001]`、`[E-001]` 和 `[E_001]` 三种写法。
- 修复高级工作台可能把兼容编号误判为无引用的问题。
## v2.3.0

### 新增与改进
- 全自动论文生成新增统一自动质量判定：自动检查事实核查、引用、参考文献、结构、摘要、字数、资料完整性和风险阈值，并明确显示是否满足开始时设定的要求。
- 质量检查未完成、AI 接口失败或资料不足时，结果会自动标记为“自动判定未达标”，不会误报通过。
- 用户数据迁移到稳定的本机目录；升级时自动合并旧版单文件和完整版数据，冲突文件保留备份，不删除原目录。
- 新增后台 GitHub Release 检查和“帮助 → 检查更新”下载入口，显示真实版本、Release 地址和网络错误。
- 修复导出目录不可用时的回退崩溃，增强 Claude/Gemini 模型请求稳定性；Windows 打包改用稳定 PowerShell 调用和 ASCII 传输文件名，发布自动化更可靠。
