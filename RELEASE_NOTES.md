### Added
- 新增“批注管理”页面，支持搜索、筛选、新增、编辑、批量删除和重新编号。
- 分章节写作页支持选中正文后直接添加批注，并自动写入批注标记。
- 新增可自定义编号前缀、分隔符、位数、类别和颜色的批注样式模板。
- 全自动导出新增 `批注表.json` 和 `批注表.md`，正文使用到的批注会进入“批注说明”附录。

### Changed
- 旧 `E001` 证据编号会无损同步到批注库，证据库与普通批注仍保持独立。
- AI 自然化和定向修改会保护批注标记，防止改写过程中丢失编号。
- 批注重新编号会同步更新各章节正文中的标记。
- 历史记录现在会关联批注表等完整导出文件。
- GitHub 下载资产统一改为中文名称，并区分“单文件版”和“完整版”。

### Fixed
- 证据引用兼容 `[E001]`、`[E-001]` 和 `[E_001]` 三种写法。
- 修复高级工作台可能把兼容编号误判为无引用的问题。
