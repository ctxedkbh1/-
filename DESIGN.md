# 论文助手设计系统

## 1. 氛围与识别

安静、可信、工作导向的 Windows 桌面研究工具。界面应让用户感觉资料、模型与论文状态都可核验、可回溯。识别特征是“蓝灰研究工作台”：深色导航负责定位，浅色内容面承载高密度任务，状态色只表达真实状态。

## 2. 颜色

| 角色 | Token | 值 | 用途 |
|---|---|---|---|
| 主背景 | surface-page | `#F5F6F8` | 应用背景 |
| 主面板 | surface-panel | `#FFFFFF` | 表单、详情面板 |
| 次面板 | surface-muted | `#F2F4F7` | 表头、筛选条、空状态 |
| 导航 | surface-nav | `#243447` | 主侧边栏 |
| 导航选中 | surface-nav-active | `#356FD3` | 当前页面 |
| 主文字 | text-primary | `#1D2939` | 标题、正文 |
| 次文字 | text-secondary | `#667085` | 说明、元数据 |
| 禁用文字 | text-disabled | `#98A2B3` | 禁用状态 |
| 边框 | border-default | `#D0D5DD` | 控件、分隔 |
| 主操作 | accent-primary | `#356FD3` | 主按钮、焦点 |
| 主操作悬停 | accent-hover | `#285FB8` | Hover |
| 成功 | status-success | `#039855` | 可用、已连接 |
| 警告 | status-warning | `#B54708` | Preview、未知 |
| 错误 | status-error | `#D92D20` | 不可用、删除 |
| 信息 | status-info | `#175CD3` | 链接、同步信息 |

规则：不使用装饰渐变；状态颜色不得替代文字；新增颜色先更新本表。

## 3. 字体

- 字体栈：`Segoe UI`, `Microsoft YaHei UI`, sans-serif
- 页面标题：24px / 600
- 区块标题：18px / 600
- 卡片标题：15px / 600
- 正文与控件：13px / 400
- 辅助文字：12px / 400
- 表格数据使用等宽数字；字距固定为 0
- 正文不得低于 12px，高 DPI 下由 Qt 自动缩放

## 4. 间距与布局

- 基础单位：4px
- 紧凑：4 / 8px；表单：12px；面板内边距：16 / 20 / 24px；区块间距：24 / 32px
- 控件高度：32px；主操作高度：36px；工具栏高度：40px
- 圆角：输入/按钮 4px；卡片 6px；对话框 8px
- 主窗口最小 900×650；小于 1100px 主侧边栏可隐藏
- AI 模型中心与参考文献中心使用 list-detail：左侧列表固定 220–280px，右侧为唯一纵向滚动区
- 1366×768 下保持双栏；宽度不足时列表与详情切换为上下或独立页
- 不允许横向滚动主内容；长 URL、Model ID、DOI 必须换行或省略

## 5. 组件

### StatusBadge
- 结构：8px 状态点 + 状态文字
- 状态：available / unknown / unavailable / not-checked / preview / deprecated
- 状态必须同时有颜色与文字，不单靠颜色

### ProviderListItem
- 内容：名称、连接状态、模型数量、最后检查时间
- 状态：默认、选中、禁用、错误
- 列表自身滚动，详情区不随列表滚动

### ModelCard
- 内容：收藏按钮、名称、Model ID、状态、能力、上下文、来源、操作
- 操作：选择、测试、详情；禁用时仍可查看详情
- 未知能力显示“未知”，禁止根据名称推断

### ReferenceCard
- 内容：标题、作者、年份、来源、唯一标识、同步来源、验证状态
- 操作：查看、收藏、加入论文、插入引用
- 缺失字段明确标记，不自动补造

### SearchBar / FilterBar
- 搜索即时过滤本地 Registry，不触发网络
- 筛选可清空；长筛选项允许换行

### DetailPanel
- 顶部固定标题与主操作，正文是唯一滚动区
- 表单错误就地显示，不只使用弹窗

### Loading / Empty / Error
- Loading：骨架行或明确进度文字，按钮禁用
- Empty：说明下一步并提供单一主操作
- Error：错误摘要、HTTP 状态（如有）、重试操作；不得显示 Key

## 6. 动效与交互

- 按钮反馈 120ms；页面/详情切换 180ms
- 只使用颜色、透明度和轻微位移；不做弹跳、晃动、连续缩放
- 刷新模型、同步 Zotero 使用非阻塞 Worker 和可取消进度
- 所有交互必须支持键盘焦点；禁用动画不影响信息读取

## 7. 深度与表面

- 策略：浅色 tonal-shift + 细边框
- 普通卡片不使用大阴影；仅对话框和浮层使用轻阴影
- 禁止卡片嵌套卡片；页面区块保持无框或单层面板

## 8. 可访问性与已接受债务

### 约束
- 目标：WCAG 2.2 AA；正文对比度至少 4.5:1
- 所有输入有可见标签；所有按钮有可见焦点
- 状态不依赖颜色；高 DPI 100%–200% 不裁切
- 空、长文本、无空格 Model ID/URL 都必须通过布局压力测试

### 已接受债务
| 项目 | 位置 | 原因 | 退出条件 |
|---|---|---|---|
| 旧页面存在内联 QSS | `gui/` 多文件 | 保持增量开发，避免一次性视觉重写 | 触及页面时迁入统一主题 |
| 大型页面文件 | `advanced_workspace.py`, `auto_mode_page.py` | 当前业务稳定，不在基础阶段拆全文件 | 新功能只放新组件，逐步抽离 |
| Qt offscreen 无系统字体 | CI/离屏截图 | offscreen 插件报告字体数量为 0，只能验证结构和非空白 | 发布前在真实 Windows 会话检查中文字体 |
